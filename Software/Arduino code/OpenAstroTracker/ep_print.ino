/*
 * 
void printMenu(String line)
{
  for (int i = 0;i < line.length();i++) {
    if (line[i] == '>') {
      lcd.write(rightArrow);
    }
    else if (line[i] == '<') {
      lcd.write(leftArrow);
    }
    else {
      lcd.print(line[i]);
    }
  }
}
*/
